## Agent skills

### Issue tracker

Issues live in GitHub Issues of `pintman/cscircles-docker` (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
